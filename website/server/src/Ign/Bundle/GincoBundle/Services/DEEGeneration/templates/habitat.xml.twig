{# Habitat #}
{# check on not-voidable attribute is defined #}
{% if habitat.refhabitat %}
    <cont:habitat>
        <cont:HabitatType>
            {% if habitat.codehabref %}
                <cont:codeHabRef>{{ habitat.codehabref }}</cont:codeHabRef>
            {% endif %}
            <cont:versionRef>{{ habitat.versionrefhabitat }}</cont:versionRef>
            {% if habitat.codehabitat %}
                <cont:codeHabitat>{{ habitat.codehabitat }}</cont:codeHabitat>
            {% endif %}
            <cont:refHabitat>{{ habitat.refhabitat }}</cont:refHabitat>
        </cont:HabitatType>
    </cont:habitat>
{% endif %}