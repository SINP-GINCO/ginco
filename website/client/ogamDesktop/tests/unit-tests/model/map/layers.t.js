// Test unitaire de la classe "user"
StartTest(function(t) {
	t.diag("User");
	
	var myLayer = new OgamDesktop.model.map.Layer();

	t.done(); // Optional, marks the correct exit point from the test
})