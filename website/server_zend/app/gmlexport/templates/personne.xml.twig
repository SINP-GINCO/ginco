{# Personne #}
<cont:PersonneType>
    <cont:organisme>
        <cont:OrganismeType>
            <cont:nomOrganisme>{{ personne.nomorganisme }}</cont:nomOrganisme>
        </cont:OrganismeType>
    </cont:organisme>
    {% if personne.mail %}
        <cont:mail>{{ personne.mail }}</cont:mail>
    {% endif %}
    <cont:identite>{{ personne.identite }}</cont:identite>
</cont:PersonneType>